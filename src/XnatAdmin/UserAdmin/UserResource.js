import Requestable from '../../Common/Requestable';

/**
 * User Resources API
 */
export default class UserResource extends Requestable {
  /**
   * Constructor
   * @param {JsXnat} jsXnat
   */
  constructor(jsXnat) {
    super(jsXnat);
  }
}
